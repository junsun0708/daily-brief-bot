services:
  daily-brief-bot:
    build: .
    container_name: daily-brief-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ~/.claude:/root/.claude
    user: root
